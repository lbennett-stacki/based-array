{
  "name": "based-array",
  "version": "1.0.1",
  "description": "A correct and based array implementation. 1-based indexing. obviously.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "perf": "ts-node src/perf.ts",
    "test": "vitest",
    "prepublish": "npm run build"
  },
  "files": [
    "dist"
  ],
  "author": "Luke Bennett <lbennett@fastmail.com>",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "vitest": "^1.6.0"
  }
}
